function Ball() {

    // current positoin
    this.position = {
        x: 0,
        y: 0
    };

    // ball size
    this.size = {
        height: 30,
        width: 30
    };

    // velocity vector
    this.velocity = {
        dx: 0,
        dy: 0
    };

}
